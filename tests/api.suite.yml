actor: ApiTester
modules:
    enabled:
        - Asserts
        - Symfony:
              app_path: 'src'
              environment: 'test'
        - REST:
              url: /api/v1/
              depends: Symfony